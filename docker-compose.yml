services:
  docs:
    image: ${TEAL_DOCS_IMAGE:-ghcr.io/platypus27/teal-docs:local}
    build:
      context: .
      dockerfile: apps/docs/Dockerfile
    ports:
      - "127.0.0.1:8087:80"
    restart: unless-stopped
