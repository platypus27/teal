# Generated documentation artifacts

The files in this directory are generated by `apps/docs/scripts` from the
package source and the documentation registry. Run `npm run generate` to
refresh them; CI runs the corresponding `check:generated` commands to detect
drift.
